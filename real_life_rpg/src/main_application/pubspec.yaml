name: main_application
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  english_words: ^4.0.0
  provider: ^6.0.0
  #flutter_secure_storage: ^9.2.2  # Use the latest version
  percent_indicator: ^4.0.0
  intl: ^0.19.0
  crypto: ^3.0.0
  camera: ^0.11.0+1
  path_provider: ^2.1.3
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true